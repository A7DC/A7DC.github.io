import React from 'react'

const ContentContainer = ({ children }) => (
  <container className='flex flex-column items-center justify-center'>
    {children}
  </container>
)

export default ContentContainer
