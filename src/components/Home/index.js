import React from 'react'
import { animated } from 'react-spring'

export const Home = (styles, defaultStyles) => <animated.div style={{ ...defaultStyles, ...styles, backgroundColor: '#247BA0' }}>Home</animated.div>

